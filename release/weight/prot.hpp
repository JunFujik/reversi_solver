#ifndef PROT_HPP
#define PROT_HPP

#include <iostream>
#include <string>
#include <vector>

using vest = std::vector<std::string>;
using veve_int = std::vector<std::vector<int>>;

#define NEGA_INF -1000000

struct inin{
    int a;
    int b;
};

struct ininin{
    int a;
    int b;
    int c;
};

#endif